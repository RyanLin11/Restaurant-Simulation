import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * The machine where money is stored.
 * 
 * @author Rachel Tong
 * @version 2 (11.10)
 */
public class Register extends Actor
{
    /**
     * Creates a Register.
     * @param width the width of the register image.
     * @param height the height of the register image.
     */
    public Register(int width, int height)
    {
        getImage().scale(width, height);
    }
}
